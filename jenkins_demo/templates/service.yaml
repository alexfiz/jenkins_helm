apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins_demo.fullname" . }}
  labels:
    {{- include "jenkins_demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins_demo.selectorLabels" . | nindent 4 }}
